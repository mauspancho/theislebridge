[project]
name = "theisle-server-bridge"
version = "0.1.0"
description = "Local REST bridge for The Isle Evrima server administration"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
theisle-bridge = "theisle_bridge.cli:main"

[tool.setuptools]
packages = ["theisle_bridge", "theisle_bridge.commands"]
